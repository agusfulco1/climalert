spring:
  application:
    name: climalert
  profiles:
    active: local
  mail:
    host: smtp.gmail.com
    port: 587
    username: "clave falsa"
    password: "clave falsa"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

weatherapi:
  url: "http://api.weatherapi.com/v1/"
  key: "clave falsa"  # Usar otro archivo application-local.yaml
  location: "Buenos Aires"

climalert:
  destinatarios: destinatario1,destinatario2,destinatario3
